Description: Use a different file for testing instead of unavailable Cargo.lock
 .
 The transfer-encoding check in async_impl_file_part() was removed in upstream
 commit 449f251bd2311a8ba73d550c6da3d3ff39d04ce8.
Last-Update: 2024-11-02
--- a/tests/multipart.rs
+++ b/tests/multipart.rs
@@ -125,15 +125,15 @@
     let _ = env_logger::try_init();
 
     let form = reqwest::blocking::multipart::Form::new()
-        .file("foo", "Cargo.lock")
+        .file("foo", "LICENSE-APACHE")
         .unwrap();
 
-    let fcontents = std::fs::read_to_string("Cargo.lock").unwrap();
+    let fcontents = std::fs::read_to_string("LICENSE-APACHE").unwrap();
 
     let expected_body = format!(
         "\
          --{0}\r\n\
-         Content-Disposition: form-data; name=\"foo\"; filename=\"Cargo.lock\"\r\n\
+         Content-Disposition: form-data; name=\"foo\"; filename=\"LICENSE-APACHE\"\r\n\
          Content-Type: application/octet-stream\r\n\r\n\
          {1}\r\n\
          --{0}--\r\n\
@@ -182,16 +182,16 @@
     let _ = env_logger::try_init();
 
     let form = reqwest::multipart::Form::new()
-        .file("foo", "Cargo.lock")
+        .file("foo", "LICENSE-APACHE")
         .await
         .unwrap();
 
-    let fcontents = std::fs::read_to_string("Cargo.lock").unwrap();
+    let fcontents = std::fs::read_to_string("LICENSE-APACHE").unwrap();
 
     let expected_body = format!(
         "\
          --{0}\r\n\
-         Content-Disposition: form-data; name=\"foo\"; filename=\"Cargo.lock\"\r\n\
+         Content-Disposition: form-data; name=\"foo\"; filename=\"LICENSE-APACHE\"\r\n\
          Content-Type: application/octet-stream\r\n\r\n\
          {1}\r\n\
          --{0}--\r\n\
