--- a/Cargo.toml
+++ b/Cargo.toml
@@ -640,8 +640,8 @@
 version = "0.6.0"
 optional = true
 
-[target."cfg(windows)".dependencies.windows-registry]
-version = "0.4"
+#[target."cfg(windows)".dependencies.windows-registry]
+#version = "0.4"
 
 [lints.rust.unexpected_cfgs]
 level = "warn"
