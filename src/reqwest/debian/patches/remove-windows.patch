--- a/Cargo.toml
+++ b/Cargo.toml
@@ -616,2 +616,2 @@
-[target."cfg(windows)".dependencies.windows-registry]
-version = "0.2"
+#[target."cfg(windows)".dependencies.windows-registry]
+#version = "0.2"
