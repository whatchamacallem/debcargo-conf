Description: Disable hickory-dns feature and related deps
 There is some problem in hickory-proto.
 .
 trust-dns feature is a leftover when trust-dns was renamed hickory-dns.
Forwarded: not-needed
Last-Update: 2024-11-02
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -272 +272 @@
-hickory-dns = ["dep:hickory-resolver"]
+#hickory-dns = ["dep:hickory-resolver"]
@@ -331 +331 @@
-trust-dns = []
+#trust-dns = []
@@ -373,4 +373,4 @@
-[target.'cfg(not(target_arch = "wasm32"))'.dependencies.hickory-resolver]
-version = "0.24"
-features = ["tokio-runtime"]
-optional = true
+#[target.'cfg(not(target_arch = "wasm32"))'.dependencies.hickory-resolver]
+#version = "0.24"
+#features = ["tokio-runtime"]
+#optional = true
