overlay = "."
uploaders = ["Matthias Geiger <werdahias@debian.org>"]
# Some tests are written in C and get built when `cargo test` is executed.
whitelist = ["tests/*.c"]

# neede to re-generate the code, do not remove
[source]
build_depends = ["gir-rust-code-generator (>= 0.21)","gir1.2-graphene-1.0-dev","gobject-introspection (>= 1.86.0-3~)","libgraphene-1.0-dev","xmlstarlet"]
[packages.lib]
depends = ["libgraphene-1.0-dev"]
[packages."lib+v1_12"]
test_is_broken = true
