overlay = "."
uploaders = ["Matthias Geiger <werdahias@debian.org>"]
# Some tests are written in C and get built when `cargo test` is executed.
whitelist = ["tests/*.c"]

[source]
build_depends_arch = ["gir-rust-code-generator (>= 0.20)", "gir1.2-graphene-1.0-dev", "gobject-introspection (>= 1.86.0-3~)", "xmlstarlet"]

[packages.lib]
depends = ["libgraphene-1.0-dev"]

[packages."lib+v1_12"]
test_is_broken = true
