--- rust-copypasta-0.7.1.orig/Cargo.toml
+++ rust-copypasta-0.7.1/Cargo.toml
@@ -32,7 +32,7 @@ x11 = ["x11-clipboard"]
 #optional = true
 
 [target."cfg(all(unix, not(any(target_os=\"macos\", target_os=\"android\", target_os=\"ios\", target_os=\"emscripten\"))))".dependencies.x11-clipboard]
-version = "0.5.1"
+version = "0.6"
 optional = true
 # [target."cfg(target_os = \"macos\")".dependencies.objc]
 # version = "0.2"
--- rust-copypasta-0.7.1.orig/src/x11_clipboard.rs
+++ rust-copypasta-0.7.1/src/x11_clipboard.rs
@@ -15,7 +15,7 @@
 use std::marker::PhantomData;
 use std::time::Duration;
 
-use x11_clipboard::xcb::xproto::Atom;
+use x11_clipboard::xcb::x::Atom;
 use x11_clipboard::{Atoms, Clipboard as X11Clipboard};
 
 use crate::common::*;
