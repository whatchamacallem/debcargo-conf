Index: breezyshim/Cargo.toml
===================================================================
--- breezyshim.orig/Cargo.toml
+++ breezyshim/Cargo.toml
@@ -56,10 +56,6 @@ version = ">=0.2"
 features = ["python-debian"]
 optional = true
 
-[dependencies.dirty-tracker]
-version = "0.3"
-optional = true
-
 [dependencies.lazy-regex]
 version = ">=2"
 
@@ -111,4 +107,3 @@ debian = [
     "dep:debian-control",
 ]
 default = ["auto-initialize"]
-dirty-tracker = ["dep:dirty-tracker"]
