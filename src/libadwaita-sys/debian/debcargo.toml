overlay = "."
uploaders = ["Matthias Geiger <matthias.geiger1024@tutanota.de>"]
collapse_features = true
whitelist = ["tests/*.c"]

# The C tests are broken for this library
[packages.lib]
depends = ["libadwaita-1-dev"]
test_is_broken = true
