overlay = "."
uploaders = ["Matthias Geiger <werdahias@debian.org>"]
collapse_features = true
whitelist = ["tests/*.c"]

# needed to regenerate the code, do not remove
[source]
build_depends = ["libadwaita-1-dev (>= 1.7~beta)","libgirepository1.0-dev","gir-rust-code-generator (>= 0.20.5-3)","xmlstarlet","libgraphene-1.0-dev"]
[packages.lib]
test_depends = ["libadwaita-1-dev (>= 1.7~beta)"]
