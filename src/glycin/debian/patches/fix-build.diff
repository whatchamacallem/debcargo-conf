--- a/src/util.rs
+++ b/src/util.rs
@@ -57,7 +57,7 @@
             };
             cicp_params.set_range(range);
 
-            Ok(cicp_params.build_color_state()?)
+            Ok(cicp_params.build_color_state()?.expect("REASON"))
         }
     }
 }
