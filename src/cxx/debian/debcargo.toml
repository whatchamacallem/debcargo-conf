overlay = "."
uploaders = ["Matthias Geiger <werdahias@debian.org>"]
collapse_features = true

[packages.lib]
test_is_broken = true
# Keep in sync with patch
# There is no librust-cxxbridge-cmd; just a binary
depends = ["cxxbridge-cmd (>= 1.0.141-~~)"]
test_depends = ["g++"]
[packages."lib+alloc"]
test_is_broken = false
[packages."lib+default"]
test_is_broken = false
[packages."lib.std"]
test_is_broken = false

