overlay = "."
uploaders = ["Antonin Delpeuch <antonin@delpeuch.eu>"]
excludes = [
        # Benchmarks depend on unpackaged crates, skip them
        "benches/**",
        # This test depends on the specifics of the rust toolchain, skip it
        "tests/ui/matrix.rs",
        "tests/ui/matrix.stderr"
]
