overlay = "."
uploaders = ["Federico Ceratto <federico@debian.org>"]
collapse_features = true

[packages.lib]
depends = ["libseat-dev"]
[packages."lib+use_bindgen"]
# Tries to write to a system directory
test_is_broken = true
