Index: serialport/Cargo.toml
===================================================================
--- serialport.orig/Cargo.toml
+++ serialport/Cargo.toml
@@ -143,9 +143,6 @@ version = "1.0.0"
 version = "0.12.0"
 default-features = false
 
-[dev-dependencies.rstest_reuse]
-version = "0.6.0"
-
 [dev-dependencies.rustversion]
 version = "1.0.16"
 
Index: serialport/tests/test_baudrate.rs
===================================================================
--- serialport.orig/tests/test_baudrate.rs
+++ serialport/tests/test_baudrate.rs
@@ -3,7 +3,7 @@ mod config;
 //use assert_hex::assert_eq_hex;
 //use config::{hw_config, HardwareConfig};
 use rstest::rstest;
-use rstest_reuse::{self, apply, template};
+//use rstest_reuse::{self, apply, template};
 use serialport::{ClearBuffer, SerialPort};
 use std::ops::Range;
 use std::time::Duration;
@@ -42,7 +42,7 @@ fn check_test_message(sender: &mut dyn S
     assert_eq!(recv_buf, send_buf, "Received message does not match sent");
 }
 
-#[template]
+/*#[template]
 #[rstest]
 #[case(9600)]
 #[case(57600)]
@@ -54,7 +54,7 @@ fn standard_baud_rates(#[case] baud: u32
 #[case(1000)]
 #[case(42000)]
 #[case(100000)]
-fn non_standard_baud_rates(#[case] baud: u32) {}
+fn non_standard_baud_rates(#[case] baud: u32) {}*/
 
 /// Test cases for setting the baud rate via [`SerialPortBuilder`].
 mod builder {
