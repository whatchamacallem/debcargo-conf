overlay = "."
uploaders = [
  "Ximin Luo <infinity0@debian.org>",
  "Sylvestre Ledru <sylvestre@debian.org>",
  "kpcyrd <git@rxv.cc>",
]

[source]
build_depends = ["debhelper (>= 13)", "pandoc", "bash-completion", "fish-common", "pkgconf"]

[packages.bin]
summary = "Modern replacement for ls"
section = "utils"
extra_lines = ["Replaces: exa (<< 0.14.1-1)", "Breaks: exa (<< 0.14.1-1)"]

description = """
eza is an improved file lister with more features and better defaults.
It uses colours to distinguish file types and metadata. It knows about
symlinks, extended attributes, and Git. And it’s small, fast, and just
one single binary.
"""

[packages.lib]
# Disable because cannot find build-script-build (not sure where
# this is coming from)
# https://github.com/ogham/eza/issues/651
test_is_broken = true
