overlay = "."
uploaders = [
  "kpcyrd <git@rxv.cc>",
  "Holger Levsen <holger@debian.org>"
]
excludes = ["debian/**"]

[source]
section = "net"
build_depends = ["help2man"]

[packages.bin]
summary = "Monitor and reproduce binary packages from source - Reproducible Builds"
section = "net"
