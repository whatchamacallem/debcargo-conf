overlay = "."
uploaders = ["Matthias Geiger <werdahias@riseup.net>"]
collapse_features = true
# Some tests are written in C and get built when `cargo test` is executed.
whitelist = ["tests/*.c"]
# needed for regeneration, do not edit
[source]
build_depends = ["libgirepository1.0-dev", "libharfbuzz-dev","gir-rust-code-generator (>= 0.19.1)", "xmlstarlet"]
[packages.lib]
depends = ["libpango1.0-dev"]
