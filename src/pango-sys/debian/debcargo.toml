overlay = "."
uploaders = ["Matthias Geiger <werdahias@debian.org>"]
# Some tests are written in C and get built when `cargo test` is executed.
whitelist = ["tests/*.c"]
# needed for regeneration, do not edit
[source]
build_depends = ["libgirepository1.0-dev", "libpango1.0-dev (>= 1.56.3)", "libharfbuzz-dev","gir-rust-code-generator (>= 0.21)", "xmlstarlet"]
[packages.lib]
depends = ["libpango1.0-dev"]
