overlay = "."
uploaders = ["Matthias Geiger <matthias.geiger1024@tutanota.de>"]
collapse_features = true

#the async-io feature requires a runtime to also be enabled
[packages."lib+async-io"]
test_is_broken = true

[packages."lib+runtime-async-std"]
test_is_broken = false

[packages."lib+runtime-smol"]
test_is_broken = false

[packages."lib+@"]
test_is_broken = false
