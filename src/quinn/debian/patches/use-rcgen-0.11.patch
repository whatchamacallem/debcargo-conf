--- a/Cargo.toml
+++ b/Cargo.toml
@@ -149,7 +149,7 @@
 version = "0.8"
 
 [dev-dependencies.rcgen]
-version = "0.13"
+version = "0.11"
 
 [dev-dependencies.rustls-pemfile]
 version = "2"
--- a/examples/server.rs
+++ b/examples/server.rs
@@ -100,13 +100,13 @@
             Err(ref e) if e.kind() == io::ErrorKind::NotFound => {
                 info!("generating self-signed certificate");
                 let cert = rcgen::generate_simple_self_signed(vec!["localhost".into()]).unwrap();
-                let key = PrivatePkcs8KeyDer::from(cert.key_pair.serialize_der());
-                let cert = cert.cert.into();
+                let key = PrivatePkcs8KeyDer::from(cert.get_key_pair().serialize_der());
+                let cert = cert.serialize_der().unwrap();
                 fs::create_dir_all(path).context("failed to create certificate directory")?;
                 fs::write(&cert_path, &cert).context("failed to write certificate")?;
                 fs::write(&key_path, key.secret_pkcs8_der())
                     .context("failed to write private key")?;
-                (cert, key.into())
+                (CertificateDer::from(cert), key.into())
             }
             Err(e) => {
                 bail!("failed to read certificate: {}", e);
--- a/examples/common/mod.rs
+++ b/examples/common/mod.rs
@@ -58,8 +58,8 @@
 fn configure_server(
 ) -> Result<(ServerConfig, CertificateDer<'static>), Box<dyn Error + Send + Sync + 'static>> {
     let cert = rcgen::generate_simple_self_signed(vec!["localhost".into()]).unwrap();
-    let cert_der = CertificateDer::from(cert.cert);
-    let priv_key = PrivatePkcs8KeyDer::from(cert.key_pair.serialize_der());
+    let cert_der = CertificateDer::from(cert.serialize_der().unwrap());
+    let priv_key = PrivatePkcs8KeyDer::from(cert.get_key_pair().serialize_der());
 
     let mut server_config =
         ServerConfig::with_single_cert(vec![cert_der.clone()], priv_key.into())?;
--- a/src/tests.rs
+++ b/src/tests.rs
@@ -43,7 +43,7 @@
     // Avoid NoRootAnchors error
     let cert = rcgen::generate_simple_self_signed(vec!["localhost".into()]).unwrap();
     let mut roots = RootCertStore::empty();
-    roots.add(cert.cert.into()).unwrap();
+    roots.add(CertificateDer::from(cert.serialize_der().unwrap())).unwrap();
 
     let mut client_config = crate::ClientConfig::with_root_certificates(Arc::new(roots)).unwrap();
     const IDLE_TIMEOUT: Duration = Duration::from_millis(500);
@@ -80,7 +80,7 @@
     // Avoid NoRootAnchors error
     let cert = rcgen::generate_simple_self_signed(vec!["localhost".into()]).unwrap();
     let mut roots = RootCertStore::empty();
-    roots.add(cert.cert.into()).unwrap();
+    roots.add(CertificateDer::from(cert.serialize_der().unwrap())).unwrap();
 
     let mut endpoint =
         Endpoint::client(SocketAddr::new(IpAddr::V4(Ipv4Addr::LOCALHOST), 0)).unwrap();
@@ -267,7 +267,7 @@
 
 /// Constructs endpoints suitable for connecting to themselves and each other
 struct EndpointFactory {
-    cert: rcgen::CertifiedKey,
+    cert: rcgen::Certificate,
     endpoint_config: EndpointConfig,
 }
 
@@ -284,14 +284,14 @@
     }
 
     fn endpoint_with_config(&self, transport_config: TransportConfig) -> Endpoint {
-        let key = PrivateKeyDer::Pkcs8(self.cert.key_pair.serialize_der().into());
+        let key = PrivateKeyDer::Pkcs8(self.cert.get_key_pair().serialize_der().into());
         let transport_config = Arc::new(transport_config);
         let mut server_config =
-            crate::ServerConfig::with_single_cert(vec![self.cert.cert.der().clone()], key).unwrap();
+            crate::ServerConfig::with_single_cert(vec![CertificateDer::from(self.cert.serialize_der().unwrap())], key).unwrap();
         server_config.transport_config(transport_config.clone());
 
         let mut roots = rustls::RootCertStore::empty();
-        roots.add(self.cert.cert.der().clone()).unwrap();
+        roots.add(CertificateDer::from(self.cert.serialize_der().unwrap())).unwrap();
         let mut endpoint = Endpoint::new(
             self.endpoint_config.clone(),
             Some(server_config),
@@ -479,8 +479,8 @@
         // We don't use the `endpoint` helper here because we want two different endpoints with
         // different addresses.
         let cert = rcgen::generate_simple_self_signed(vec!["localhost".into()]).unwrap();
-        let key = PrivatePkcs8KeyDer::from(cert.key_pair.serialize_der());
-        let cert = CertificateDer::from(cert.cert);
+        let key = PrivatePkcs8KeyDer::from(cert.get_key_pair().serialize_der());
+        let cert = CertificateDer::from(cert.serialize_der().unwrap());
         let mut server_config =
             crate::ServerConfig::with_single_cert(vec![cert.clone()], key.into()).unwrap();
 
@@ -666,8 +666,8 @@
     //let _guard = subscribe();
 
     let cert = rcgen::generate_simple_self_signed(vec!["localhost".into()]).unwrap();
-    let key = PrivatePkcs8KeyDer::from(cert.key_pair.serialize_der());
-    let cert = CertificateDer::from(cert.cert);
+    let key = PrivatePkcs8KeyDer::from(cert.get_key_pair().serialize_der());
+    let cert = CertificateDer::from(cert.serialize_der().unwrap());
 
     let mut roots = rustls::RootCertStore::empty();
     roots.add(cert.clone()).unwrap();
--- a/tests/many_connections.rs
+++ b/tests/many_connections.rs
@@ -154,8 +154,8 @@
 fn gen_cert() -> (CertificateDer<'static>, PrivatePkcs8KeyDer<'static>) {
     let cert = rcgen::generate_simple_self_signed(vec!["localhost".to_string()]).unwrap();
     (
-        cert.cert.into(),
-        PrivatePkcs8KeyDer::from(cert.key_pair.serialize_der()),
+        CertificateDer::from(cert.serialize_der().unwrap()),
+        PrivatePkcs8KeyDer::from(cert.get_key_pair().serialize_der()),
     )
 }
 
