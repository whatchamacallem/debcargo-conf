--- a/Cargo.toml
+++ b/Cargo.toml
@@ -64,9 +64,3 @@
 default = ["std"]
 std = []
 
-[target."cfg(loom)".dependencies.loom]
-version = "0.7"
-optional = true
-
-[target."cfg(target_family = \"wasm\")".dev-dependencies.wasm-bindgen-test]
-version = "0.3"
