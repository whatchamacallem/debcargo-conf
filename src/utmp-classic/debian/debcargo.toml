overlay = "."
uploaders = ["Sylvestre Ledru <sylvestre@debian.org>"]
collapse_features = true

[packages.lib]
# Require at least 0.1.3-3, since that version has a functional zerocopy 0.8 patch
depends = ["librust-utmp-classic-raw-0.1+default-dev (>= 0.1.3-3~)"]
