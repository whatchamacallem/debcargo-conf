overlay = "."
uploaders = ["Reinhard Tartler <siretart@tauware.de>"]

# enable on next upstraem release, can drop patch related to iptables
# excludes = ["**/examples"]

# test break on examples
[packages.lib]
test_is_broken = true

