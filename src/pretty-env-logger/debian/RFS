it's a dependency of cargo, which would pull in an outdated env-logger now that
0.7.1 is packaged. since cargo's packaging re-uses the patches from
debcargo-conf, packaging it properly makes patching and tracking easier.
