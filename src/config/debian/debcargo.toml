overlay = "."
uploaders = ["Alexander Kjäll <alexander.kjall@gmail.com>"]
collapse_features = true

[packages.lib]
# uses winnow::error::ModalResult, only added in winnow 0.6.26
depends = ['librust-winnow-dev (>= 0.6.25)']
