--- a/Cargo.toml
+++ b/Cargo.toml
@@ -64,3 +64,3 @@
 
-[target.'cfg(all(target_arch = "wasm32", wasm_bindgen_unstable_test_coverage))'.dependencies.minicov]
+[disabled.target.'cfg(all(target_arch = "wasm32", wasm_bindgen_unstable_test_coverage))'.dependencies.minicov]
 version = "0.3"
