overlay = "."
uploaders = ["Fabian Grünbichler <debian@fabian.gruenbichler.email>"]
collapse_features = true
summary = "C-compatible API for zlib-rs, the memory-safe, pure Rust implementation of zlib."
