Index: blake2-rfc/Cargo.toml
===================================================================
--- blake2-rfc.orig/Cargo.toml
+++ blake2-rfc/Cargo.toml
@@ -22,7 +22,7 @@ categories = ["cryptography", "no-std"]
 license = "MIT OR Apache-2.0"
 repository = "https://github.com/cesarb/blake2-rfc"
 [dependencies.arrayvec]
-version = "0.4.6"
+version = "0.7"
 default-features = false
 
 [dependencies.clippy]
Index: blake2-rfc/src/blake2.rs
===================================================================
--- blake2-rfc.orig/src/blake2.rs
+++ blake2-rfc/src/blake2.rs
@@ -305,7 +305,7 @@ macro_rules! blake2_impl {
 
 #[cfg_attr(feature = "clippy", allow(cast_possible_truncation))]
 #[cold]
-pub fn selftest_seq(len: usize) -> ArrayVec<[u8; 1024]> {
+pub fn selftest_seq(len: usize) -> ArrayVec::<u8, 1024> {
     use core::num::Wrapping;
 
     let seed = Wrapping(len as u32);
