overlay = "."
uploaders = [
  "kpcyrd <git@rxv.cc>",
  "Holger Levsen <holger@debian.org>"
]

[source]
section = "net"
build_depends = ["help2man"]

[packages.bin]
summary = "Worker agent for rebuilderd server"
recommends = [
  "devscripts",
  "diffoscope",
]
