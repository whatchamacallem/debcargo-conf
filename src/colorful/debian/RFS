The package can be uploaded as copyright was clarified.
This package is only needed by ascii_table, but this dep
was temporarily patched-out. The patch can be removed,
but it creates a strong dependency, between packages,
maybe it is better to remove the patch later at some point.
