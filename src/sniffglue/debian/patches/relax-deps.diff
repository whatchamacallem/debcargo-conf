--- a/Cargo.toml
+++ b/Cargo.toml
@@ -55,4 +55,4 @@

-[dependencies.ansi_term]
-version = "0.12"
+[dependencies.nu-ansi-term]
+version = "0.50"
 
@@ -95,7 +95,7 @@ version = "0.2"
 version = "0.4"
 
 [dependencies.nix]
-version = "0.29"
+version = ">= 0.27, < 1.0"
 features = [
     "fs",
     "user",
--- a/src/fmt.rs
+++ b/src/fmt.rs
@@ -1,6 +1,6 @@
 use std::sync::Arc;
 
-use ansi_term::Color;
+use nu_ansi_term::Color;
 use bstr::ByteSlice;
 use sha2::{Sha512, Digest};
 use std::cmp;
