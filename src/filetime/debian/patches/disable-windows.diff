--- a/Cargo.toml
+++ b/Cargo.toml
@@ -42,6 +42,6 @@
-[target."cfg(windows)".dependencies.windows-sys]
-version = "0.52.0"
-features = [
-    "Win32_Foundation",
-    "Win32_Storage_FileSystem",
-]
+#[target."cfg(windows)".dependencies.windows-sys]
+#version = "0.52.0"
+#features = [
+#    "Win32_Foundation",
+#    "Win32_Storage_FileSystem",
+#]
