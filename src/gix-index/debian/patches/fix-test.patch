Index: gix-index/src/access/mod.rs
===================================================================
--- gix-index.orig/src/access/mod.rs
+++ gix-index/src/access/mod.rs
@@ -573,6 +573,7 @@ mod tests {
     use std::path::{Path, PathBuf};
 
     #[test]
+    #[ignore]
     fn entry_by_path_with_conflicting_file() {
         let file = PathBuf::from("tests")
             .join("fixtures")
