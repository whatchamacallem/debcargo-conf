overlay = "."
uploaders = ["Michael R. Crusoe <crusoe@debian.org>"]
excludes = ["xz/**"]
collapse_features = true

[source]
build_depends = ["liblzma-dev (>= 5.8.0)"]

[packages.lib]
depends = ["liblzma-dev (>= 5.8.0)"]
summary = "Raw bindings to liblzma - Rust source code"
description = """
 Contains an implementation of LZMA and xz stream encoding/decoding.
.
Source code for Debianized Rust crate "liblzma-sys" 
"""
