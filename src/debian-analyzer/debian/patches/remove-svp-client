Index: debian-analyzer/Cargo.toml
===================================================================
--- debian-analyzer.orig/Cargo.toml
+++ debian-analyzer/Cargo.toml
@@ -159,10 +159,6 @@ version = ">=0.8"
 features = ["postgres"]
 optional = true
 
-[dependencies.svp-client]
-version = "0.2.0"
-optional = true
-
 [dependencies.tempfile]
 version = "3"
 
@@ -192,5 +188,4 @@ default = [
 ]
 merge3 = ["dep:merge3"]
 python = []
-svp = ["dep:svp-client"]
 udd = ["dep:sqlx"]
