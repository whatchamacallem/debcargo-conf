Index: rustls-pemfile/Cargo.toml
===================================================================
--- rustls-pemfile.orig/Cargo.toml
+++ rustls-pemfile/Cargo.toml
@@ -41,6 +41,7 @@ required-features = ["std"]
 name = "benchmark"
 path = "benches/benchmark.rs"
 harness = false
+required-features = ["std"]
 
 [dependencies.pki-types]
 version = "1.9"
