overlay = "."
uploaders = ["Fabian Grünbichler <debian@fabian.gruenbichler.email>", "Michael Tokarev <mjt@tls.msk.ru>"]

# --all-feature enables xen+postcopy for which vhost complains
[packages."lib+@"]
test_is_broken = true

[source]
# rust-vmm is explicitly 64bit-only
build_depends = ["architecture-is-64-bit"]

