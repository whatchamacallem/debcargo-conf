--- a/src/typefind.rs
+++ b/src/typefind.rs
@@ -246,6 +246,7 @@ mod tests {
     use super::*;
 
     #[test]
+    #[ignore = "broken in debian"]
     fn test_typefind_call_function() {
         crate::init().unwrap();
 
