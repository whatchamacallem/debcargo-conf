overlay = "."
uploaders = ["Antonin Delpeuch <antonin@delpeuch.eu>"]
collapse_features = true

excludes = [
  # Full text of licenses, used by the 'text' feature, currently disabled
  # because the full text have all different copyright statuses
  "src/text/*"
]
