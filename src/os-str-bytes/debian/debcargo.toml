overlay = "."
uploaders = ["Sylvestre Ledru <sylvestre@debian.org>"]

[packages."lib+nightly"]
test_is_broken = true

# error: The 'OS_STR_BYTES_CHECKED_CONVERSIONS' environment variable must be defined to use the 'checked_conversions' feature.
[packages."lib+checked_conversions"]
test_is_broken = true

# error: The 'memchr' feature is useless when 'raw_os_str' is disabled; it should be disabled too.
[packages."lib+memchr"]
test_is_broken = true

