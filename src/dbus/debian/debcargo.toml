overlay = "."
uploaders = ["Wolfgang Silbermayr <wolfgang@silbermayr.at>"]

# The auto-generated summary is too long.
summary = "Bindings to D-Bus, a bus commonly used on Linux for IPC"
collapse_features = true
[packages.lib]
# Disable because upstream has not updates tests after an API break
test_is_broken = true
