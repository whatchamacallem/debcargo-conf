--- a/Cargo.toml
+++ b/Cargo.toml
@@ -45,10 +45,3 @@ version = "1"
 [dependencies.home]
 version = "0.5"
 
-[target."cfg(windows)".dependencies.windows-sys]
-version = "0.59"
-features = [
-    "Win32_Foundation",
-    "Win32_System_Com",
-    "Win32_UI_Shell",
-]
