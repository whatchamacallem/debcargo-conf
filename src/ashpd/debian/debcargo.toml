overlay = "."
uploaders = ["Henry-Nicolas Tourneur <debian@nilux.be>", "Matthias Geiger <matthias.geiger1024@tutanota.de>"]
collapse_features = true

# tests fail if no backend is enabled
[packages.lib]
test_is_broken = true

[packages."lib+async-std"]
test_is_broken = false

[packages."lib+default"]
test_is_broken = false

[packages."lib+tokio"]
test_is_broken = false
