overlay = "."
uploaders = ["Andreas Henriksson <andreas@fatal.se>"]

[packages.lib]
# test needs a default sound card configured, which is usually not available
# in build chroots.
test_is_broken = true
