--- a/Cargo.toml
+++ b/Cargo.toml
@@ -162,5 +162,5 @@
     "dep:wait-timeout",
     "dep:libc",
-    "dep:windows-sys",
+    #"dep:windows-sys",
 ]
 color = [
@@ -201,6 +201,6 @@
 optional = true
 
-[target."cfg(windows)".dependencies.windows-sys]
-version = "0.48.0"
-features = ["Win32_Foundation"]
-optional = true
+#[target."cfg(windows)".dependencies.windows-sys]
+#version = "0.48.0"
+#features = ["Win32_Foundation"]
+#optional = true
