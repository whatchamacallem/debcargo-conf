overlay = "."
uploaders = ["Jelmer Vernooĳ <jelmer@debian.org>"]
whitelist = ["**/*.c"]
excludes = ["jemalloc/**", "configure/**"]

[source]
build_depends = ["libjemalloc-dev"]

[packages.lib]
depends = ["libjemalloc-dev"]
