overlay = "."
uploaders = ["kpcyrd <git@rxv.cc>"]
excludes = ["xz-*/**"]
# The auto-generated summary is too long.
summary = "Bindings to liblzma for lzma and xz stream encoding/decoding"

[packages.lib]
depends = ["liblzma-dev"]
test_is_broken = false
[packages."lib+default"]
test_is_broken = false
[packages."lib+static"]
test_is_broken = true
