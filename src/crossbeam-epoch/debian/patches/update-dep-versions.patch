Comment: safe to drop "nightly" feature since nothing depends on it, and it is
 removed in the next upstream version (0.8) anyways
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -23,7 +23,7 @@
 license = "MIT/Apache-2.0"
 repository = "https://github.com/crossbeam-rs/crossbeam"
 [dependencies.arrayvec]
-version = "0.4"
+version = "0.5"
 default-features = false
 
 [dependencies.cfg-if]
@@ -49,6 +49,5 @@
 [features]
 alloc = ["crossbeam-utils/alloc"]
 default = ["std"]
-nightly = ["crossbeam-utils/nightly", "arrayvec/use_union"]
 sanitize = []
 std = ["crossbeam-utils/std", "lazy_static"]
