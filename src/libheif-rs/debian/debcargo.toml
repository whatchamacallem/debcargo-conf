overlay = "."
uploaders = ["Fab Stz <fabstz-it@yahoo.fr>", "Matthias Geiger <werdahias@riseup.net>"]

summary = "Safe wrapper around libheif-sys to parse heif/heic files"

collapse_features = true

[packages.lib]
test_is_broken = true

# Only the tests for the appropriate libheif version would work
[packages."lib+v1_19"]
test_is_broken = false
