Subject: Drop Android-related Features

These are not going to be built on Debian.

--- a/Cargo.toml
+++ b/Cargo.toml
@@ -104,11 +104,9 @@
 version = "1"
 
 [features]
-__internal_bench = []
 alloc = []
 clock = [
     "iana-time-zone",
-    "android-tzdata",
     "now",
 ]
 default = [
@@ -152,10 +150,6 @@
 version = "0.2"
 optional = true
 
-[target.'cfg(target_os = "android")'.dependencies.android-tzdata]
-version = "0.1.1"
-optional = true
-
 [target."cfg(unix)".dependencies.iana-time-zone]
 version = "0.1.45"
 features = ["fallback"]
