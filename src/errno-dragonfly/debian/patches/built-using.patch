--- a/build.rs
+++ b/build.rs
@@ -1,7 +1,11 @@
 extern crate cc;
 
+use std::env;
+
 fn main() {
     cc::Build::new()
         .file("src/errno.c")
         .compile("liberrno.a");
+
+    println!("dh-cargo:deb-built-using=errno=0={}", env::var("CARGO_MANIFEST_DIR").unwrap());
 }
