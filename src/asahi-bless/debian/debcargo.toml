overlay = "."
uploaders = ["Andreas Henriksson <andreas@fatal.se>", "NoisyCoil <noisycoil@tutanota.com>"]

whitelist = [ "**/offsets.c" ]

[packages.bin]
section = "utils"
summary = "select the active boot volume on ARM Macs"
description = """
This tool can be used on Apple Silicon Macs to list boot volumes and select the
active one in a permanent way or for next boot only.
"""
