Description: Remove dependency to loom in tests
Author: Antonin Delpeuch <antonin@delpeuch.eu>
---
This patch header follows DEP-3: http://dep.debian.net/deps/dep3/
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -39,10 +39,6 @@ name = "ecow"
 path = "src/lib.rs"
 
 [[test]]
-name = "loom"
-path = "tests/loom.rs"
-
-[[test]]
 name = "tests"
 path = "tests/tests.rs"
 
@@ -59,6 +55,3 @@ default-features = false
 default = ["std"]
 std = []
 
-[target."cfg(loom)".dependencies.loom]
-version = "0.7"
-optional = true
