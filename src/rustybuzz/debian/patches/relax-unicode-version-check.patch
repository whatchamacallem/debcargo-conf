--- a/src/hb/unicode.rs
+++ b/src/hb/unicode.rs
@@ -839,6 +839,7 @@
 #[cfg(test)]
 mod tests {
     #[test]
+    #[ignore = "we can't guarantee this"]
     fn check_unicode_version() {
         assert_eq!(unicode_bidi_mirroring::UNICODE_VERSION, (15, 1, 0));
         assert_eq!(unicode_ccc::UNICODE_VERSION, (15, 0, 0));
