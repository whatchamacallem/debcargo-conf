overlay = "."
uploaders = ["Matthias Geiger <werdahias@riseup.net>"]
whitelist = ["tests/*.c"]
collapse_features = true
# this is needed for regeneration, do not delete
[packages.lib]
depends = ["libgtk-4-dev (>= 4.10.4)","libgraphene-1.0-dev","libgirepository1.0-dev","gir-rust-code-generator (>= 0.18.2)","xmlstarlet"]

[packages."lib+v4_12"]
test_is_broken = true
