overlay = "."
uploaders = ["Henry-Nicolas Tourneur <debian@nilux.be>", "Matthias Geiger <matthias.geiger1024@tutanota.de>"]
excludes = ["tests/*.c"]
collapse_features = true
[packages.lib]
depends = ["libgtk-4-dev"]
# gtk4-sys crate is testing too many symbols and has known upstream bugs
test_is_broken = true
