overlay = "."
uploaders = ["Matthias Geiger <werdahias@riseup.net>"]
whitelist = ["tests/constant.c", "tests/layout.c"]
collapse_features = true
# this is needed for regeneration, do not remove.
[source]
build_depends = ["libgstreamer-plugins-base1.0-dev","libgstreamer1.0-dev","libgirepository1.0-dev","gir-rust-code-generator (>= 0.18.3)","xmlstarlet"]
# currently broken because the C library is too old
[packages."lib+v1_24"]
test_is_broken = true

