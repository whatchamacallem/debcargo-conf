--- a/Cargo.toml
+++ b/Cargo.toml
@@ -45,7 +45,6 @@
 real-sys = [
     "dep:env_home",
     "dep:rustix",
-    "dep:winsafe",
 ]
 regex = ["dep:regex"]
 tracing = ["dep:tracing"]
@@ -83,7 +82,3 @@
 version = "0.1.0"
 optional = true
 
-[target."cfg(windows)".dependencies.winsafe]
-version = "0.0.19"
-features = ["kernel"]
-optional = true

