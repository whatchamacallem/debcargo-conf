overlay = "."
uploaders = ["kpcyrd <git@rxv.cc>"]
summary = "Test a phone for stalkerware using adb and usb debugging"


[source]
build_depends = ["adb", "help2man"]

[packages.bin]
section = "utils"
recommends = ["ca-certificates"]
depends = ["adb"]
