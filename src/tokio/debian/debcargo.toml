overlay = "."
uploaders = ["Wolfgang Silbermayr <wolfgang@silbermayr.at>",
             "kpcyrd <git@rxv.cc>"]
collapse_features = true

# The auto-generated summary is too long.
summary = "Event-driven, non-blocking I/O platform"

[packages.lib]
test_is_broken = true
test_depends = ["strace"]
breaks = ["librust-actix-server-dev (<< 2.5.1)"]

[packages."lib+full"]
test_is_broken = false
