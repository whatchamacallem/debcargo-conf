overlay = "."
uploaders = ["Matthias Geiger <werdahias@riseup.net>"]
collapse_features = true
# Some tests are written in C and get built when `cargo test` is executed.
whitelist = ["tests/*.c"]
# needed to regenerate the code, do not remove
[source]
build_depends = ["libgstreamer1.0-dev","libgirepository1.0-dev","gir-rust-code-generator (>= 0.19.0)","xmlstarlet"]
# broken because of older C library
[packages.lib]
test_depends = ["libgstreamer1.0-dev"]
[packages."lib+v1_24"]
test_is_broken = true
