overlay = "."
uploaders = ["Ximin Luo <infinity0@debian.org>"]

# The auto-generated summary is too long.
summary = "Run pkg-config at build time Cargo build scripts"

[packages.lib]
depends = ["pkg-config"]
