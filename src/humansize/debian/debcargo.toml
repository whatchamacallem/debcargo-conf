overlay = "."
uploaders = ["kpcyrd <git@rxv.cc>"]

# The auto-generated summary is too long.
summary = "Represent file sizes in a human-readable format"

[packages."lib+no_alloc"]
test_is_broken = true
