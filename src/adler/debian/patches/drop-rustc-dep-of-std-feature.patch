Index: adler/Cargo.toml
===================================================================
--- adler.orig/Cargo.toml
+++ adler/Cargo.toml
@@ -44,16 +44,6 @@ file = "src/lib.rs"
 replace = "https://docs.rs/adler/{{version}}"
 search = "https://docs.rs/adler/[a-z0-9\\.-]+"
 
-[dependencies.compiler_builtins]
-version = "0.1.2"
-optional = true
-
-[dependencies.core]
-version = "1.0.0"
-optional = true
-package = "rustc-std-workspace-core"
-
 [features]
 default = ["std"]
-rustc-dep-of-std = ["core", "compiler_builtins"]
 std = []
