;; /etc/kanata/config.kbd kanata configuration file
;;
;; Find example configuration files in /usr/share/doc/kanata/examples/

(defcfg
;; Set to "yes" to process unmapped keys through kanata
process-unmapped-keys no
)

;; Empty mapping
(defsrc)

;; Empty layer
(deflayer empty)
