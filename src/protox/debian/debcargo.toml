overlay = "."
uploaders = ["Blair Noctis <ncts@debian.org>"]

collapse_features = true

# The bin is currently a simple program that compiles a set of source files
# to a file descriptor set. Not that useful by itself.
bin = false
