overlay = "."
uploaders = ["Matthias Geiger <werdahias@riseup.net>"]
collapse_features = true
[packages.lib]
depends = ["rustc (>= 1.70)"]
# needed for now until trybuild2 gets accepted 
test_is_broken = true
