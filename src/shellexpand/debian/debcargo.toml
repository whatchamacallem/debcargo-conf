overlay = "."
uploaders = ["Carlos F. Sanz <carlos@sanz.dev>"]
collapse_features = true
allow_prerelease_deps = true
# tests fail when the base-0 feature isn't present
[packages.lib]
test_is_broken = true
[packages."lib+path"]
test_is_broken = true
[packages."lib+tilde"]
test_is_broken = true
