--- a/src/test.rs
+++ b/src/test.rs
@@ -347,6 +347,7 @@
 }
 
 #[test]
+#[cfg(not(target_arch = "x86", "arm", "powerpc64"))]
 fn test_idle_timeout() {
     static DROPPED: AtomicUsize = AtomicUsize::new(0);
 
