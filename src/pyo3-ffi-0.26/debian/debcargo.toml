overlay = "."
uploaders = ["Jelmer Vernooĳ <jelmer@debian.org>"]
collapse_features = true
semver_suffix = true

[packages.lib]
depends = ["python3-all-dev"]

# Re-enable this once Python 3.14 is the default in Debian.
[packages."lib+abi3-py314"]
test_is_broken = true
