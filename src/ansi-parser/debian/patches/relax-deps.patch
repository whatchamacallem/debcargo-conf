diff --git a/Cargo.toml b/Cargo.toml
index ab153ea..ba9cdfc 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -19,10 +19,10 @@ description = "A library using nom for parsing ANSI Escape Codes"
 homepage = "https://gitlab.com/DavidBittner/ansi-parser"
 license = "MPL-2.0"
 [dependencies.heapless]
-version = "0.5.6"
+version = "0.7"
 
 [dependencies.nom]
-version = "4.2.3"
+version = "4"
 default-features = false
 
 [features]
diff --git a/src/enums.rs b/src/enums.rs
index 556ceb2..c031897 100644
--- a/src/enums.rs
+++ b/src/enums.rs
@@ -1,7 +1,7 @@
 #[cfg(test)]
 mod tests;
 
-use heapless::{Vec, consts::U5};
+use heapless::Vec;
 
 ///The following are the implemented ANSI escape sequences. More to be added.
 #[derive(Debug, PartialEq, Clone)]
@@ -16,7 +16,7 @@ pub enum AnsiSequence {
     CursorRestore,
     EraseDisplay,
     EraseLine,
-    SetGraphicsMode(Vec<u8, U5>),
+    SetGraphicsMode(Vec<u8, 5>),
     SetMode(u8),
     ResetMode(u8),
     HideCursor,
