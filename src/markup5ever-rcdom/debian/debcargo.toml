overlay = "."
uploaders = ["Henry-Nicolas Tourneur <debian@nilux.be>"]

# The auto-generated summary is too long
summary="DOM structure for use by tests in html5ever/xml5ever"

[packages.lib]
# Missing dev-dep rustc-test (not packageable, outdated, does not compile)
test_is_broken = true
