overlay = "."
uploaders = ["Blair Noctis <ncts@debian.org>"]

[packages.lib]
# tests::with_default_pager fails when it's the last test being run
# https://gitlab.com/imp/pager-rs/-/issues/15
test_is_broken = true
