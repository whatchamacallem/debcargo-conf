overlay = "."
uploaders = ["Daniel Kahn Gillmor <dkg@fifthhorseman.net>"]
collapse_features = true
description = """This crate offers a few utilities to make working with the types
and traits in the bytes crate (librust-bytes-dev) even more
convenient and powerful.  It contains:

- SegmentedBuf for concatenation without copying
- Str and StrMut, string wrappers around Bytes and BytesMut

It can be built without std if needed.
"""
