overlay = "."
uploaders = ["Fabian Grünbichler <f.gruenbichler@proxmox.com>"]
collapse_features = true

# bundled copy of libstd, and bindings that are presumablly autogenerated from it.
excludes = ["zstd/**","src/bindings_*"]

[packages.lib]
depends = ["libzstd-dev"]
