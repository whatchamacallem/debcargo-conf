overlay = "."
uploaders = [
  "Ximin Luo <infinity0@debian.org>",
  "kpcyrd <git@rxv.cc>",
]
excludes = ["src/android-api.c"]

# The auto-generated summary is too long.
summary = "Find shared libraries loaded in the current process"
