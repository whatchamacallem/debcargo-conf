overlay = "."
uploaders = ["Matthias Geiger <matthias.geiger1024@tutanota.de>"]
allow_prerelease_deps = true
# Needed to pass autopkgtest
excludes = ["examples/*.rs"]

[packages."lib+deterministic-timestamps"]
test_is_broken = true
