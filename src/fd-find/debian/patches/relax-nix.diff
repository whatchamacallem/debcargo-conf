Index: fd-find/Cargo.toml
===================================================================
--- fd-find.orig/Cargo.toml
+++ fd-find/Cargo.toml
@@ -133,10 +133,10 @@ default = [
 #optional = true
 
 [target."cfg(all(unix, not(target_os = \"redox\")))".dependencies.libc]
-version = "0.2"
+version = "0"
 
 [target."cfg(unix)".dependencies.nix]
-version = "0.24.2"
+version = "0"
 features = ["signal"]
 default-features = false
 
