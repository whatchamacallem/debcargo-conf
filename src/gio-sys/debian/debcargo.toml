overlay = "."
uploaders = ["Matthias Geiger <werdahias@debian.org>"]
collapse_features = true
# Some tests are written in C and get built when `cargo test` is executed.
whitelist = ["tests/*.c"]
[source]
build_depends = ["gir-rust-code-generator (>= 0.21)", "gir1.2-gio-2.0-dev", "gobject-introspection (>= 1.86.0-3~)", "xmlstarlet"]
[packages.lib]
test_depends = ["libglib2.0-dev (>= 2.86)"]
