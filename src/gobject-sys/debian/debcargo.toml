overlay = "."
uploaders = ["Matthias Geiger <werdahias@debian.org>"]
collapse_features = true
# Some tests are written in C and get built when `cargo test` is executed.
whitelist = ["tests/*.c"]

[source]
build_depends_arch = ["gir-rust-code-generator (>= 0.20)", "gir1.2-gobject-2.0-dev", "gobject-introspection (>= 1.86.0-3~)", "xmlstarlet"]

[packages.lib]
depends = ["libglib2.0-dev (>= 2.86)"]
