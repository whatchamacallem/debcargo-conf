overlay = "."
uploaders = ["Jelmer Vernooĳ <jelmer@debian.org>"]
collapse_features = true

bin = true
bin_name = "upstream-ontologist"

[packages.bin]
# python3-upstream-ontologist used to ship a binary with the same name
conflicts = ["python3-upstream-ontologist (<<0.1.44)"]
replaces = ["python3-upstream-ontologist (<<0.1.44)"]
breaks = ["python3-upstream-ontologist (<<0.1.44)"]
section = "devel"
summary = "find metadata about upstream software projects"
description = """
The upstream ontologist provides a common interface for finding metadata about
upstream software projects.

It will gather information from any sources available, prioritize data that it
has higher confidence in as well as report the confidence for each of the
bits of metadata.
"""

[packages.lib]
test_is_broken = true

[packages."lib+cli"]
test_is_broken = true

[packages."lib+debcargo"]
test_is_broken = true

[packages."lib+debian"]
test_is_broken = true

[packages."lib+dist-ini"]
test_is_broken = true

[packages."lib+git-config"]
test_is_broken = true

[packages."lib+launchpad"]
test_is_broken = true

[packages."lib+opam"]
test_is_broken = true

[packages."lib+pyo3"]
test_is_broken = true

[packages."lib+pyproject-toml"]
test_is_broken = true

[packages."lib+python-pkginfo"]
test_is_broken = true

[packages."lib+r-description"]
test_is_broken = true

[packages."lib+setup-cfg"]
test_is_broken = true
