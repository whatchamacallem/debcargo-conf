overlay = "."
uploaders = ["kpcyrd <git@rxv.cc>"]

# The auto-generated summary is too long.
summary = "Network adapters and network configuration for windows"

[packages.lib]
# crate only supports windows.
test_is_broken = true

