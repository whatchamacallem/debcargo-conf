overlay = "."
uploaders = ["Matthias Geiger <werdahias@riseup.net>"]
[packages.bin]
section="rust"
summary = "Automatically generates Rust FFI bindings to C and C++ libraries."
description = """
bindgen-cli can automatically generates Rust FFI bindings to C and C++ libraries.
The executable is named bindgen.
"""
extra_lines = ["Replaces: rust-bindgen (<< 0.66.1-1)","Breaks: rust-bindgen (<< 0.66.1-1)"]
