overlay = "."
uploaders = ["Ximin Luo <infinity0@debian.org>"]
collapse_features = true

# Disable all tests until upstream includes the tests in the crate again
# https://github.com/bitflags/bitflags/pull/387
[packages.lib]
test_is_broken = true

[packages."lib+core"]
test_is_broken = true

[packages."lib+rustc-dep-of-std"]
test_is_broken = true
