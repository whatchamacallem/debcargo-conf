Index: version-ranges/src/lib.rs
===================================================================
--- version-ranges.orig/src/lib.rs
+++ version-ranges/src/lib.rs
@@ -1214,6 +1214,7 @@ pub mod tests {
 
         #[cfg(feature = "serde")]
         #[test]
+        #[ignore]
         fn serde_round_trip(range in proptest_strategy()) {
             let s = ron::ser::to_string(&range).unwrap();
             let r = ron::de::from_str(&s).unwrap();
