Index: phf-generator/Cargo.toml
===================================================================
--- phf-generator.orig/Cargo.toml
+++ phf-generator/Cargo.toml
@@ -25,9 +25,6 @@ description = "PHF generation logic"
 [[bench]]
 name = "benches"
 harness = false
-[dependencies.criterion]
-version = "=0.3.4"
-optional = true
 
 [dependencies.phf_shared]
 version = "0.10.0"
@@ -36,5 +29,3 @@ version = "0.8.0"
 [dependencies.rand]
 version = "0.8"
 features = ["small_rng"]
-[dev-dependencies.criterion]
-version = "=0.3.4"
Index: phf-generator/benches/benches.rs
===================================================================
--- phf-generator.orig/benches/benches.rs
+++ phf-generator/benches/benches.rs
@@ -1,4 +1,4 @@
-use criterion::measurement::Measurement;
+/*use criterion::measurement::Measurement;
 use criterion::{criterion_group, criterion_main, Bencher, BenchmarkId, Criterion};
 
 use rand::distributions::Standard;
@@ -77,4 +77,7 @@ mod rayon {
     gen_hash_xlarge
 );
 
-criterion_main!(benches);
+criterion_main!(benches);*/
+fn main() {
+    println!("bench disabled because criterion is not in Debian");
+}
