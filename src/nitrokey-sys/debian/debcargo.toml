overlay = "."
uploaders = ["Wolfgang Silbermayr <wolfgang@silbermayr.at>"]

excludes = ["libnitrokey-*"]

# The auto-generated summary is too long.
summary = "Low-level bindings to libnitrokey"

[packages.lib]
depends = ["libnitrokey-dev","libhidapi-dev"]
