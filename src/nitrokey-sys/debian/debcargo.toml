overlay = "."
uploaders = ["Wolfgang Silbermayr <wolfgang@silbermayr.at>", "Robin Krahl <robin.krahl@ireas.org>"]

excludes = ["libnitrokey-*"]

# The auto-generated summary is too long.
summary = "low-level bindings for libnitrokey"

[packages.lib]
depends = ["libnitrokey-dev"]
