overlay = "."
uploaders = ["Matthias Geiger <werdahias@riseup.net>"]
collapse_features = true
# Some tests are written in C and get built when `cargo test` is executed.
whitelist = ["tests/*.c"]
# needed to regenerate the code, do not remove
[source]
build_depends = ["libatk1.0-dev", "gir-rust-code-generator", "xmlstarlet", "libgirepository1.0-dev"]
[packages.lib]
test_depends = ["libatk1.0-dev"]

#currently broken because the C library is too old
[packages."lib+v2_50"]
test_is_broken = true
