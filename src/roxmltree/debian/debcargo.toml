overlay = "."
uploaders = ["Andrej Shadura <andrewsh@debian.org>"]
collapse_features = true

excludes = ["testing-tools/**"]

[packages.lib]
test_is_broken = true
[packages."lib+positions"]
test_is_broken = true
