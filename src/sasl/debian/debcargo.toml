overlay = "."
uploaders = ["Martin <debacle@debian.org>"]
summary = "crate which handles SASL authentication"
description = """
A crate which handles SASL authentication.
Currently only does the client side.
Still unstable until 1.0.0.
It can be used in no_std environments.
"""
