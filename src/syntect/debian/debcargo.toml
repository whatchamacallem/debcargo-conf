overlay = "."
uploaders = ["Paride Legovini <paride@debian.org>"]

# The auto-generated summary is too long.
summary = "Syntax highlighting and code intelligence using Sublime Text's grammars"

#tests seem to have feature requirements
[packages.lib]
test_is_broken = true

[packages."lib+default"]
test_is_broken = false

[packages."lib+default-fancy"]
test_is_broken = false

[packages."lib+default-onig"]
test_is_broken = false

[packages."lib+@"]
test_is_broken = false
