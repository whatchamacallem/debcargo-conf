overlay = "."
uploaders = ["Antonin Delpeuch <antonin@delpeuch.eu>"]
collapse_features = true

excludes = [
  # Benchmarks require the 'divan' crate, which isn't packaged,
  # and they wouldn't run reliably in Debian's build pipeline anyway.
  "benches/**",
]
