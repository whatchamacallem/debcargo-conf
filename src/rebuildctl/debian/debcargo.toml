overlay = "."
uploaders = [
  "kpcyrd <git@rxv.cc>",
  "Holger Levsen <holger@debian.org>"
]

[source]
section = "utils"
build_depends = ["ca-certificates", "help2man"]
test_depends = ["ca-certificates"]

[packages.bin]
recommends = ["ca-certificates"]
summary = "Client for rebuilderd server"
