overlay = "."
uploaders = ["Maytham Alsudany <maytham@debian.org>"]

[packages.bin]
section = "utils"
summary = "tool to format hurl files"
description = """
hurlfmt is a command line tool to format hurl files and to export them to
JSON/HTML.
"""
