overlay = "."
uploaders = ["Sylvestre Ledru <sylvestre@debian.org>"]
collapse_features = true

# because of rust-openssh-sftp-client
[source]
build_depends = ["architecture-is-64-bit"]

[packages.lib]
test_is_broken = true

[packages."lib+default"]
test_is_broken = false
