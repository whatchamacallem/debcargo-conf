Updating trash from 3.* to 5.* shouldn't cause any issues with rdeps.
However it requires to update version of trash in
  ./src/broot/debian/patches/relax-deps.diff
  ./src/czkawka-core/debian/patches/relax-dep*
