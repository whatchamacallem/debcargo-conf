Origin: vendor
Subject: enable crypto-nettle feature
Forwarded: not-needed

The sequoia-openpgp crate needs to have one of the crypto backends selected,
so hardcode this to nettle as Debian uses the nettle backend everywhere else.

Index: sequoia-keystore-backend/Cargo.toml
===================================================================
--- sequoia-keystore-backend.orig/Cargo.toml
+++ sequoia-keystore-backend/Cargo.toml
@@ -61,6 +61,7 @@ version = "0.4.17"
 [dependencies.sequoia-openpgp]
 version = "2"
 default-features = false
+features = [ "crypto-nettle" ]
 
 [dependencies.tempfile]
 version = "3"
