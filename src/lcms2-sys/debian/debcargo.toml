overlay = "."
uploaders = ["Matthias Geiger <matthias.geiger1024@tutanota.de>"]
#exclude vendored liblcms
excludes = ["vendor/*"]
collapse_features = true
[packages.lib]
depends = ["liblcms2-dev"]
test_depends = ["liblcms2-dev"]
