overlay = "."
uploaders = ["Sylvestre Ledru <sylvestre@debian.org>"]
excludes = ["benches/*", "examples/*"]

# flate2 needs a backend but doesn't and couldn't choose any.
# note that flate2 is an implicit feature from the optional dependency.
[packages."lib+_deflate-any"]
test_is_broken = true
[packages."lib+flate2"]
test_is_broken = true
[packages."lib+deflate-flate2"]
test_is_broken = true
[packages."lib+deflate-zopfli"]
test_is_broken = true
