bin = false
overlay = "."
uploaders = ["Reinhard Tartler <siretart@tauware.de>"]

# error: features `tokio` and `async-process` are mutually exclusive
[packages."lib+@"] # --all-features
test_is_broken = true
