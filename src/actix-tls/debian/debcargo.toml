overlay = "."
uploaders = ["kpcyrd <git@rxv.cc>"]
collapse_features = true
# test_resolvers depends on `trust-dns-resolver`, which we don't have yet
# accept-* depends on gated features
excludes = [
  "tests/test_resolvers.rs",
  "tests/accept-rustls.rs",
  "tests/accept-openssl.rs",
]
