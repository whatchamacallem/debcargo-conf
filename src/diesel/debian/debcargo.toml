overlay = "."
uploaders = ["kpcyrd <git@rxv.cc>", "Matthias Geiger <werdahias@debian.org>"]

# The auto-generated summary is too long.
summary = "Safe and extensible ORM and Query Builder"
# the autopkgtest fails because the test suite would need a need 
# a database to run the tests 
[packages.lib]
test_is_broken = true
