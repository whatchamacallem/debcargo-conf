Index: sequoia-openpgp/Cargo.toml
===================================================================
--- sequoia-openpgp.orig/Cargo.toml
+++ sequoia-openpgp/Cargo.toml
@@ -54,6 +54,7 @@ required-features = ["compression-deflat
 [[bench]]
 name = "run_benchmarks"
 harness = false
+required-features = ["compression-deflate"]
 
 [dependencies.aes]
 version = "0.8"
