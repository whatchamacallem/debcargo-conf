--- a/build.rs
+++ b/build.rs
@@ -169,9 +169,7 @@
 
     // These platforms have a 32-bit `time_t`.
     if libc
-        && (arch == "arm"
-            || arch == "powerpc"
-            || arch == "mips"
+        && (arch == "mips"
             || arch == "sparc"
             || arch == "x86"
             || (arch == "aarch64" && os == "linux" && abi == Ok("ilp32".to_string())))
