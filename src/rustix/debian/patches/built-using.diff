Index: rustix/build.rs
===================================================================
--- rustix.orig/build.rs
+++ rustix/build.rs
@@ -208,6 +208,7 @@ fn link_in_librustix_outline(arch: &str,
             );
         }
     }
+    println!("dh-cargo:deb-built-using={}=0={}", name, var("CARGO_MANIFEST_DIR").unwrap());
 }
 
 fn use_thumb_mode() -> bool {
