overlay = "."
uploaders = ["Emanuele Rocca <ema@debian.org>"]
collapse_features = true

# The dev-dependencies x509-parser and rsa needed by the tests are not in
# Debian yet. Mark the tests as flaky for now.
[packages.lib]
test_is_broken = true
