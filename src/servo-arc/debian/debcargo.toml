overlay = "."
uploaders = ["kpcyrd <git@rxv.cc>"]

# The auto-generated summary is too long.
summary = "std::sync::Arc with some extra functionality"

[packages."lib+gecko_refcount_logging"]
test_is_broken = true
