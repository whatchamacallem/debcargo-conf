overlay = "."
uploaders = ["Matthias Geiger <werdahias@debian.org>"]
collapse_features = true
# Some tests are written in C and get built when `cargo test` is executed.
whitelist = ["tests/*.c"]

[source]
build_depends = ["libgdk-pixbuf-2.0-dev", "gir-rust-code-generator (>= 0.21)", "gir1.2-gdkpixbuf-2.0-dev", "gobject-introspection (>= 1.86.0-3~)", "xmlstarlet"]

[packages.lib]
depends = ["libgdk-pixbuf-2.0-dev"]
[packages."lib+v2_44"]
test_is_broken = true
