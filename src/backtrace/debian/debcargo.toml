overlay = "."
uploaders = [
  "kpcyrd <git@rxv.cc>",
  "Wolfgang Silbermayr <wolfgang@silbermayr.at>",
]
excludes = ["ci/**"]

# The auto-generated summary is too long.
summary = "Acquire a backtrace at runtime"

# Disable tests for now in experimental
[packages.lib]
test_is_broken = true
