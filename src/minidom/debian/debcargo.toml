overlay = "."
uploaders = ["Martin <debacle@debian.org>"]
collapse_features = true
summary = "minimal DOM library, targeting the subset of XML useful for XMPP"
description = """
A small, simple DOM implementation on top of rxml, targeting
exclusively the subset of XML useful for XMPP, e.g. use of namespaces
is mandatory.
"""
