overlay = "."
uploaders = ["Marc Dequènes (Duck) <Duck@DuckCorp.org>"]
collapse_features = true

# The no-default-features test fails
# upstream enforces choosing one of the features using compile_error!()
[packages."lib"]
test_is_broken = true

[packages."lib+@"]
test_is_broken = false

[packages."lib+default"]
test_is_broken = false

[packages."lib+libm"]
test_is_broken = false

[packages."lib+std"]
test_is_broken = false
