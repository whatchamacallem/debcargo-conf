overlay = "."
uploaders = ["Sylvestre Ledru <sylvestre@debian.org>"]

[packages."lib+@"]
test_is_broken = true

[packages.lib]
# Mark the tests as broken as sval isn't packaged yet
test_is_broken = true
