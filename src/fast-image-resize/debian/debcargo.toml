overlay = "."
uploaders = ["Fab Stz <fabstz-it@yahoo.fr>"]

collapse_features = true

# Tests are broken if run without for_testing feature
# See: https://github.com/Cykooz/fast_image_resize/issues/48
[packages.lib]
test_is_broken = true

[packages."lib+for_testing"]
test_is_broken = false

[packages."lib+@"]
test_is_broken = false
