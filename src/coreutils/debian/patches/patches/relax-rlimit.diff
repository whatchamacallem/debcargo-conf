Index: coreutils/Cargo.toml
===================================================================
--- coreutils.orig/Cargo.toml
+++ coreutils/Cargo.toml
@@ -378,7 +378,7 @@ walkdir = "2.2"
 atty = "0.2"
 
 [target.'cfg(target_os = "linux")'.dev-dependencies]
-rlimit = "0.4.0"
+rlimit = "0"
 
 [target.'cfg(unix)'.dev-dependencies]
 nix = "0.23.1"
