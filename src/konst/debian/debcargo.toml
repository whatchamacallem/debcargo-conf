overlay = "."
uploaders = ["Eddie Billoir <lechatp@outlook.fr>"]
collapse_features = true
summary = "Const of std functions and const parsing"
# As the dev seems to do like this for testing in const_panic, we do the same here.
[packages."lib+debug"]
test_is_broken = false
[packages.lib]
test_is_broken = true
