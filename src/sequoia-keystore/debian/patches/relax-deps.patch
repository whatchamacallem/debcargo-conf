Origin: vendor
Subject: Relax dependencies to work well with other rust crates in Debian
Forwarded: not-needed

Tweak env_logger to be the version in Debian.
Hardcode the crypto-nettle backend, as that is used everywhere else in Debian.
Remove env-filter as that doesn't exist in Debian.

Index: sequoia-keystore/Cargo.toml
===================================================================
--- sequoia-keystore.orig/Cargo.toml
+++ sequoia-keystore/Cargo.toml
@@ -65,7 +65,7 @@ version = "0.19"
 version = "5"
 
 [dependencies.env_logger]
-version = ">=0.10, <0.12"
+version = "<0.12"
 
 [dependencies.log]
 version = "0.4.17"
@@ -102,6 +102,7 @@ optional = true
 [dependencies.sequoia-openpgp]
 version = "2"
 default-features = false
+features = ["crypto-nettle"]
 
 [dependencies.thiserror]
 version = ">=1, <3"
@@ -123,7 +124,7 @@ features = ["compat"]
 version = "0.3"
 
 [dev-dependencies.env_logger]
-version = ">=0.10, <0.12"
+version = "<0.12"
 
 [dev-dependencies.test-log]
 version = "0.2.10"
@@ -134,7 +135,6 @@ default-features = false
 
 [dev-dependencies.tracing-subscriber]
 version = "0.3"
-features = ["env-filter"]
 default-features = false
 
 [build-dependencies.capnpc]
@@ -147,11 +147,3 @@ features = [
     "__implicit-crypto-backend-for-tests",
 ]
 default-features = false
-
-[target."cfg(windows)".dev-dependencies.sequoia-openpgp]
-version = "2"
-features = [
-    "crypto-cng",
-    "__implicit-crypto-backend-for-tests",
-]
-default-features = false
