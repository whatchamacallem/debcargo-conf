overlay = "."
uploaders = ["Johann Felix Soden <johfel@debian.org>"]
summary = "Library that helps you implement partial updates for structs"

[packages."lib+merge"] ## --no-default-features --features merge
test_is_broken = true

[packages."lib+@"] # --all-features
test_is_broken = false
