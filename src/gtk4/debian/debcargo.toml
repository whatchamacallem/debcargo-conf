overlay = "."
uploaders = ["Matthias Geiger <werdahias@debian.org>"]
collapse_features = true

[packages.lib]
depends = ["libgtk-4-dev (>= 4.14)"]
test_is_broken = false
test_depends = ["xvfb", "xauth", "libpango1.0-dev"]

# this is needed for regeneration, do not delete
[source]
build_depends = ["libgraphene-1.0-dev","libgirepository1.0-dev","gir-rust-code-generator (>= 0.20)","xmlstarlet"]

[packages."lib+v4_14"]
test_is_broken = true

[packages."lib+gnome_46"]
test_is_broken = true

[packages."lib+v4_16"]
test_is_broken = true

[packages."lib+unsafe-assume-initialized"]
test_is_broken = true
