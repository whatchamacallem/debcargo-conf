overlay = "."
uploaders = ["Matthias Geiger <werdahias@debian.org>"]
collapse_features = true

# since the features are patched out test fail atm
[packages.lib]
test_is_broken = true
depends = ["libxml2-dev"]
