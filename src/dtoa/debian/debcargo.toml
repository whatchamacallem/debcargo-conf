overlay = "."
uploaders = ["Ximin Luo <infinity0@debian.org>"]

# The auto-generated summary is too long.
summary = "Fast functions for printing floating-point primitives"

collapse_features = true

#no-panic only works in release mode
[packages."lib+no-panic"]
test_is_broken = true
