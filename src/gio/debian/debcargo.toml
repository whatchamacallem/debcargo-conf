overlay = "."
uploaders = ["Matthias Geiger <werdahias@debian.org>"]
collapse_features = true

# this is needed for generating the code, do not remove
[source]
build_depends = ["gir-rust-code-generator (>= 0.20)", "gir1.2-gio-2.0-dev", "gobject-introspection (>= 1.86.0-3~)", "xmlstarlet"]
