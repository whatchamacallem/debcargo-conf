Bug: https://github.com/rust-cli/anstyle/issues/138

--- a/src/auto.rs
+++ b/src/auto.rs
@@ -135,6 +135,7 @@
     /// Prefer [`AutoStream::choice`]
     ///
     /// This doesn't report what is requested but what is currently active.
+    #[cfg(feature = "auto")]
     #[inline]
     pub fn current_choice(&self) -> ColorChoice {
         match &self.inner {
