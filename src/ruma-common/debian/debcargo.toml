overlay = "."
uploaders = ["Matthias Geiger <werdahias@debian.org>"]
collapse_features = true

# autopkgtest fails for every feature except default and @
[packages.lib]
test_is_broken = true
# needed for ui tests
test_depends = ["rustc (>= 1.90)"]
[packages."lib+@"]
test_is_broken = false
[packages."lib+default"]
test_is_broken = false
