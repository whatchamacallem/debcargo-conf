Index: ognibuild/src/debian/build_deps.rs
===================================================================
--- ognibuild.orig/src/debian/build_deps.rs
+++ ognibuild/src/debian/build_deps.rs
@@ -136,6 +136,7 @@ mod tests {
     }
 
     #[test]
+    #[ignore]
     fn test_build_dependency_tie_breaker_count_empty() {
         let _temp_dir = TempDir::new().unwrap();
         let session = PlainSession::new();
@@ -147,6 +148,7 @@ mod tests {
     }
 
     #[test]
+    #[ignore]
     fn test_build_dependency_tie_breaker_break_tie_empty() {
         let _temp_dir = TempDir::new().unwrap();
         let session = PlainSession::new();
Index: ognibuild/src/session/plain.rs
===================================================================
--- ognibuild.orig/src/session/plain.rs
+++ ognibuild/src/session/plain.rs
@@ -380,6 +380,7 @@ mod tests {
     }
 
     #[cfg(feature = "breezy")]
+    #[ignore]  // requires internet access
     #[test]
     fn test_project_from_vcs() {
         // Trigger loading of test session before we mess up HOME
