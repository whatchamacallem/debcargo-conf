overlay = "."
uploaders = ["Matthias Geiger <werdahias@debian.org>"]
collapse_features = true
summary = "C++ code generator for integrating cxx into a Cargo build"

[packages.lib]
test_depends = ["g++"]
