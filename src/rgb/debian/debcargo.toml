overlay = "."
uploaders = ["Andrej Shadura <andrewsh@debian.org>"]
collapse_features = true

[packages."lib+argb"]
# one of the tests requires +as-bytes, absent in single feature test
test_is_broken = true
