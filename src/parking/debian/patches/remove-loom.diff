Index: parking/Cargo.toml
===================================================================
--- parking.orig/Cargo.toml
+++ parking/Cargo.toml
@@ -36,7 +36,3 @@ repository = "https://github.com/smol-rs
 
 [dev-dependencies.easy-parallel]
 version = "3.0.0"
-
-[target."cfg(loom)".dependencies.loom]
-version = "0.7"
-optional = true
