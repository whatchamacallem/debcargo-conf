overlay = "."
uploaders = ["kpcyrd <git@rxv.cc>"]

# The auto-generated summary is too long.
summary = "Attribute macro to require a function can't ever panic"

[packages.lib]
# Disable because cannot find a library (no big deal)
test_is_broken = true
